#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @c: the integer to computed
 * Return: the absolute value
 */
int abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
